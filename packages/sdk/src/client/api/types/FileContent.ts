// This file was auto-generated by Fern from our API Definition.

import type * as HaiAgents from "../index.js";

/**
 * A file attached to a user message: sent as base64, stored and read back as a platform URL.
 */
export interface FileContent {
    /** 'base64' when sending the file, 'url' once the platform has stored it. */
    type?: HaiAgents.FileContentType;
    /** Base64 payload when type is 'base64', download URL when type is 'url'. */
    source: string;
    /** File name, without its directory. */
    name: string;
    /** MIME type, e.g. 'application/pdf'; guessed if omitted. */
    mediaType?: string | null;
    /** Size of the file in bytes, set by the platform. */
    sizeBytes?: number | null;
}
